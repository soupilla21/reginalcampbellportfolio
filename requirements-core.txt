# Core dependencies for the Leadership Agent pipeline.
# Pin versions to match the production environment.

# Feed ingestion
feedparser==6.0.12

# LLM inference (local, via Ollama)
ollama>=0.2.0

# Vector similarity / semantic deduplication
chromadb>=1.5.0

# HTML parsing (researcher agent)
beautifulsoup4>=4.14.0

# Config / data serialization
pyyaml>=6.0

# Async HTTP (researcher agent)
httpx>=0.27.0

# API layer (SaaS Orchestrator)
fastapi>=0.115.0
uvicorn>=0.30.0

# Auth (SaaS Orchestrator — ES256/JWKS)
python-jose[cryptography]>=3.3.0
